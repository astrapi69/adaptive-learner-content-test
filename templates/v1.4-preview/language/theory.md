# Einleitung {#einleitung}

Write your explanation here in Markdown, in the **source language**
(`language_source`). Each `#` heading with a `{#anchor}` becomes a section that
`lesson.yaml` can reference via `theory.md#anchor`.

Greetings are the first thing you learn in any language. In Spanish:

- **hola** — hello
- **gracias** — thank you
- **por favor** — please

# Mehr Kontext {#mehr-kontext}

Add as many anchored sections as you need. Keep each one short and focused on the
words your exercises will test.

> Tip: link out from `lesson.yaml` with `example_url` to a live example.
